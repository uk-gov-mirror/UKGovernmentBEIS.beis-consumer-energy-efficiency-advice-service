export enum WallType {
    DontKnow,
    CavityNoInsualation,
    CavityInsulated,
    SolidNoInsulation,
    SolidInsulated,
}

export enum RoofType {
    DontKnow,
    PitchedNoInsulation,
    PitchedInsulated,
    FlatNoInsulation,
    FlatInsulated,
}

export enum GlazingType {
    DontKnow,
    Single,
    Double,
    Triple,
}