export enum WallType {
    DoNotKnow,
    CavityNoInsulation,
    CavityInsulated,
    SolidNoInsulation,
    SolidInsulated,
}

export enum RoofType {
    DoNotKnow,
    PitchedNoInsulation,
    PitchedInsulated,
    FlatNoInsulation,
    FlatInsulated,
}

export enum GlazingType {
    DoNotKnow,
    Single,
    Double,
    Triple,
}
