export interface StaticMeasure {
    iconClassName: string;
    basicInfoValue: string;
    basicInfoHeadline: string;
    measureHeadline: string;
    measureSummary: string;
    averageSavings: number;
}